namespace Mistforge
{
    public enum ComboDirection { Left, Right, Up, Down }

    public enum WeaponType { Gauntlet, Spear }

    public enum EquipSlot { Weapon, Armor, Accessory }

    public enum StatType { ATK, DEF, MaxAP, CritPct, APGainPct, ArtDmgPct }

    public enum BattlePhase { PlayerInput, Animating, EnemyTurn, Resolved }

    public enum MapKind { Town, Field }
}
